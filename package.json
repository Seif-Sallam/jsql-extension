{
  "name": "jsql-syntax",
  "displayName": "JSql Syntax Highlighting",
  "description": "SQL + Jinja2 syntax highlighting for JSql query patterns in Python",
  "version": "0.1.0",
  "publisher": "local",
  "license": "MIT",
  "scripts": {
    "playground": "node scripts/playground.js",
    "test": "node scripts/test.js"
  },
  "engines": { "vscode": "^1.74.0" },
  "categories": ["Programming Languages"],
  "activationEvents": ["*"],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "jsqlSyntax.selectTheme",
        "title": "JSql: Select Theme"
      },
      {
        "command": "jsqlSyntax.formatSQL",
        "title": "JSql: Format SQL"
      },
      {
        "command": "jsqlSyntax.manageScopes",
        "title": "JSql: Manage Schema Scopes"
      },
      {
        "command": "jsqlSyntax.scopeDebug",
        "title": "JSql: Debug Scope"
      }
    ],
    "configuration": {
      "title": "JSql Syntax",
      "properties": {
        "jsqlSyntax.theme": {
          "type": "string",
          "default": "dracula",
          "enum": ["dracula", "monokai", "one-dark"],
          "enumDescriptions": [
            "Dracula — cyan keywords, pink functions, green strings",
            "Monokai — red keywords, green functions, yellow strings",
            "One Dark — purple keywords, blue functions, green strings"
          ],
          "description": "Color theme for JSql syntax highlighting"
        },
        "jsqlSyntax.tableDefinitionFiles": {
          "type": "array",
          "scope": "resource",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Workspace-relative glob patterns for Python model files such as tables.py. Parsed __tablename__ and sa.Column definitions are used to color known SQL tables and columns."
        },
        "jsqlSyntax.scopes": {
          "type": "array",
          "scope": "resource",
          "default": [],
          "description": "Per-directory schema scopes. Each scope maps a path prefix to a set of table definition file globs. Files under the prefix will only see tables from that scope's files. Unscoped files fall back to tableDefinitionFiles.",
          "items": {
            "type": "object",
            "properties": {
              "prefix": {
                "type": "string",
                "description": "Workspace-relative directory prefix, e.g. \"libservice1\""
              },
              "files": {
                "type": "array",
                "items": { "type": "string" },
                "description": "Workspace-relative glob patterns for table definition files under this scope"
              }
            },
            "required": ["prefix", "files"]
          }
        },
        "jsqlSyntax.semanticWarnings": {
          "type": "boolean",
          "scope": "resource",
          "default": true,
          "description": "Enable schema-aware SQL warnings for unknown tables and invalid qualified columns using the loaded table definition files."
        }
      }
    }
  }
}
